services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      SECRET_KEY: "change-me-in-cloud"
      EMISSION_FACTOR_KG_PER_KWH: "0.82"
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
